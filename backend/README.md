# Backend

Backend-specific commands live here, but the main project documentation is now at the repository root:

- `../README.md` for full setup and feature overview
- `../documentation/postman_examples.md` for API examples
- `../documentation/ERD.md` for the database model

## Quick start

```bash
cd backend
npm install
npm run dev
```

## Scripts

- `npm run dev`
- `npm start`
- `npm run seed`
- `npm run check`
